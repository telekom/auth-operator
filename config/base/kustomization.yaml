apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base configuration - includes all core resources
# Use overlays (dev, production) to customize for specific environments

resources:
- ../crd
- ../rbac
- ../manager
- ../webhook

# Common labels for all resources
labels:
- includeSelectors: false
  pairs:
    app.kubernetes.io/name: auth-operator
    app.kubernetes.io/managed-by: kustomize
