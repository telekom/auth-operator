# DEPRECATED: Use overlays instead
# - config/overlays/dev for development and e2e testing
# - config/overlays/production for production deployments
#
# This file is kept for backwards compatibility with controller-runtime generators.

# Adds namespace to all resources.
namespace: auth-operator-system

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: auth-operator-

# Labels to add to all resources and selectors.
# labels:
# - includeSelectors: true
#   pairs:
#     someName: someValue

resources:
- ../crd
- ../rbac
- ../manager
- ../webhook

# configurations:
#   - prefix_rules.yaml
patches: []
