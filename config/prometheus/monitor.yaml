# Prometheus Monitor Service (Metrics)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: auth-operator
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager-metrics-monitor
  namespace: system
spec:
  endpoints:
    - path: /metrics
      port: metrics
      scheme: http
      # Note: This Kustomize ServiceMonitor only supports insecure metrics.
      # If you enable --metrics-secure, use Helm or patch this to https with TLS config.
  selector:
    matchLabels:
      control-plane: controller-manager
