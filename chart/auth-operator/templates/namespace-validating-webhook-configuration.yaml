{{- if .Values.namespaceAdmission.enabled }}
{{- $caBundle := include "auth-operator.webhookCABundle" (dict "root" . "kind" "ValidatingWebhookConfiguration" "name" (printf "%s-namespace-validating-webhook-configuration" (include "auth-operator.fullname" .))) | trim }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "auth-operator.fullname" . }}-namespace-validating-webhook-configuration
  labels:
  {{- include "auth-operator.labels" . | nindent 4 }}
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    {{- if $caBundle }}
    caBundle: {{ $caBundle | quote }}
    {{- end }}
    service:
      name: '{{ include "auth-operator.fullname" . }}-webhook-service'
      namespace: '{{ .Release.Namespace }}'
      path: /validate-v1-namespace
      port: {{ .Values.webhookServer.service.port | default 443 }}
  failurePolicy: Fail
  matchPolicy: Equivalent
  name: namespace.validating.webhook.auth.t-caas.telekom.de
  rules:
  - apiGroups:
    - ""
    apiVersions:
    - v1
    operations:
    - CREATE
    - UPDATE
    - DELETE
    resources:
    - namespaces
    scope: '*'
  sideEffects: None
  timeoutSeconds: 10
{{- end }}
