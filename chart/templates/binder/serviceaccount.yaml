{{- if .Values.global.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: binder-{{ template "authn-authz-operator.serviceAccountName" .}}
  labels:
{{ include "authn-authz-operator.labels.binder" . | indent 4 }}
{{- end }}
