{{- if .Values.global.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: idp-{{ template "authn-authz-operator.serviceAccountName" .}}
  labels:
{{ include "authn-authz-operator.labels.idp" . | indent 4 }}
{{- end }}
