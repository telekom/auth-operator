{{- if .Values.global.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: generator-{{ template "authn-authz-operator.serviceAccountName" .}}
  labels:
{{ include "authn-authz-operator.labels.generator" . | indent 4 }}
{{- end }}
