# Expected ServiceAccount auto-created by BindDefinition
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-auto-created-sa
  namespace: "{{NAMESPACE}}"
  # Labels and ownerReferences are added by the controller
