# Expected ClusterRole generated by RoleDefinition
# This file contains the core rules structure expected - metadata will vary
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: golden-cluster-reader
rules:
  # Rules are generated by the controller based on API discovery
  # The exact rules depend on what APIs are available in the cluster
  # At minimum, we expect it to NOT include restricted verbs (update, create)
